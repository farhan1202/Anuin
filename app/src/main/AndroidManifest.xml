<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anuin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".introNlogin.RegisterActivity"></activity>
        <activity android:name=".introNlogin.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".introNlogin.ApiLoginActivity" />
        <activity android:name=".introNlogin.WelcomeActivity" />
        <activity android:name=".FormPesananActivity" />
        <activity android:name=".DetailJasaActivity" />
        <activity android:name=".AddAddress" />
        <activity android:name=".introNlogin.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>